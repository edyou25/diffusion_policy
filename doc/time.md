## 当前训练时长预估（基于 2025-12-03 18:08 这次 run）

- **开始时间**：2025-12-03 18:08:49（`hydra.run.dir = 2025.12.03/18.08.49_*`）
- **最新日志时间**：2025-12-03 20:53:17（W&B `debug-core.log` 的父进程退出时间）
- **已完成进度**：epoch 300 / 3050（global_step ≈ 5.06×10⁴，约 9.8%）
- **实际用时**：约 2 小时 45 分
- **平均耗时**：
  - 每个 epoch ≈ 33 秒
  - 每个 batch（168/epoch）≈ 0.20 秒

### 剩余时间推算（假设速率保持稳定）
| 目标 | 剩余 epoch | 预计剩余时间 |
|------|-----------|-------------|
| 500 epoch（阶段性检查） | 200 | ≈ 1.8 小时 |
| 1000 epoch | 700 | ≈ 6.4 小时 |
| 2000 epoch | 1,700 | ≈ 15.6 小时 |
| 3050 epoch（全程） | 2,750 | ≈ 25.2 小时（约 1.05 天） |

> 说明：以上估算基于当前 batch_size 与 data loader 配置，期间 roll-out、验证与 checkpoint 的额外耗时已包含在平均值里；若后续调参（如减 worker、增验证频率）需要重新估算。

### 预计电力消耗（粗略估算）
- 假设训练期间整机功耗约 250W（GPU≈150W + CPU/内存/散热≈100W）。
- 按平均速率再训练 25.2 小时：`0.25 kW × 25.2 h ≈ 6.3 kWh`。
- 若电价 ￥0.8/kWh，费用约 **￥5**；若 ￥1.0/kWh，则约 **￥6.3**。

---

当前时刻的 GPU/CPU 资源使用情况如下：

### GPU
- **显存占用**：总 12 GB 中用掉约 8.4 GB。训练进程 `python train.py` 占用 6.9 GB，桌面相关进程（Xorg、gnome-shell、浏览器等）共占约 1.5 GB。
- **利用率**：`GPU-Util` 约 96%，说明此刻 CUDA 核心在密集计算（训练正处于前向/反向传播阶段）。功耗 147 W、温度 64 °C，属于正常负载。
- **进程列表**：PID 13964 的 `python` 为训练主进程，其余都是桌面程序。无需额外动作，GPU 正在高效工作。

### CPU/内存
- **整体负载**：`htop` 显示系统内存 31 GB，当前占用 24.2 GB（≈ 78%），交换分区已用 4.4 GB，说明 dataloader 和浏览器等进程占用较高，系统开始依赖 swap。Load average 3.37/3.49/3.00，表示 CPU 使用率不低但仍在可控范围。
- **训练进程**：`python train.py` 进程有多个 worker（PID 13964、14283 等），每个进程常驻内存约 12.8 GB，CPU 使用率高峰在 70～80%，说明数据加载和模型计算都在活跃运行。
- **其它进程**：浏览器（Edge）等图形界面程序占用一些 CPU/内存（每个十几到几百 MB），但相较训练本身影响不算大。

总结：目前 GPU 在高负载训练状态，内存占用接近上限并出现 swap。整体虽能正常运行，但若想留出更多安全余量，可以考虑调低 dataloader num_workers、关闭部分浏览器/窗口或增大物理内存，以避免因 swap 过多造成卡顿。
