name: guide_lowdim

n_lookahead: 10
k_lookahead: 5
frame_stride: 5
n_obstacle_circles: 6
n_obstacle_segments: 6
obstacle_dim: ${eval:'${task.n_obstacle_circles}*3 + ${task.n_obstacle_segments}*4'}
obs_dim: ${eval:'4 + 2*${task.n_lookahead} + ${task.obstacle_dim}'}  # robot(2) + human(2) + lookahead + obstacles
action_dim: 2  # forward_delta, heading_delta
keypoint_dim: 0

dataset:
  _target_: diffusion_policy.dataset.guide_lowdim_dataset.GuideLowdimDataset
  data_dir: /home/yyf/IROS2026/FollowDataset/data  # Can be overridden via: task.dataset.data_dir=/path/to/data
  horizon: ${horizon}
  pad_before: ${eval:'${n_obs_steps}-1+${n_latency_steps}'}
  pad_after: ${eval:'${n_action_steps}-1'}
  seed: 42
  val_ratio: 0.05
  max_train_episodes: null
  action_mode: forward_heading  # delta, forward_heading, position, or velocity
  frame_stride: ${task.frame_stride}
  n_lookahead: ${task.n_lookahead}
  k_lookahead: ${task.k_lookahead}
  robot_frame: true  # robot-centric obs/action; prevents memorizing global route
  robot_state: vel   # robot-centric self state (2D): vel (vx,vy) or zero
  turn_speed: 1.5    # rad/s, used to clip heading_delta when timestamps exist
  n_obstacle_circles: ${task.n_obstacle_circles}
  n_obstacle_segments: ${task.n_obstacle_segments}
  obstacle_include_radius: true
  segment_repr: closest_dir  # endpoints | closest_dir

env_runner:
  _target_: diffusion_policy.env_runner.guide_lowdim_runner.GuideLowdimRunner
  data_dir: ${task.dataset.data_dir}
  n_test: 50
  n_test_vis: 4
  test_start_seed: 100000
  horizon: ${horizon}
  n_obs_steps: ${n_obs_steps}
  n_action_steps: ${n_action_steps}
  pad_before: ${eval:'${n_obs_steps}-1+${n_latency_steps}'}
  pad_after: ${eval:'${n_action_steps}-1'}
  val_ratio: ${task.dataset.val_ratio}
  action_mode: ${task.dataset.action_mode}
  frame_stride: ${task.frame_stride}
  n_lookahead: ${task.n_lookahead}
  k_lookahead: ${task.k_lookahead}
  robot_frame: ${task.dataset.robot_frame}
  n_obstacle_circles: ${task.n_obstacle_circles}
  n_obstacle_segments: ${task.n_obstacle_segments}
  obstacle_include_radius: ${task.dataset.obstacle_include_radius}
  tqdm_interval_sec: 5.0
